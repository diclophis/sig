<?php

class SiG_Model {
   /*
   var $registeredModels;

   function Instance ()
   {
      static $model;

      if ($model == NULL) {
         $model = new SiG_Model();
      }

      return $model;
   }

   function Register ($name, $class)
   {
      $this->registeredModels[$name] = $class;
   }

   function GetModels ()
   {
      $class = array();
      foreach (get_declared_classes() as $class) {
         if (is_subclass_of(
   }
   */
}

?>
